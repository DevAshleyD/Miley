dist: trusty
env:
  - DOCKER_COMPOSE_VERSION=1.4.2
language: python
python:
  - "3.6"
sudo: required
services:
  - docker
before_install:
  - ls -lah
  - docker-compose ps
script:
  - docker-compose run web python manage.py test
